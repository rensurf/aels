[tool.ruff]
[tool.ruff.lint]
ignore = [
  "BLE001",  # Lambda handlers intentionally catch Exception to return 500 errors
]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[[tool.mypy.overrides]]
module = ["agent_framework", "agent_framework.*"]
ignore_errors = true
follow_imports = "skip"
